<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>URL Shortener</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f7f9fc;
    }
    .hero {
      padding: 80px 20px;
      text-align: center;
      background: linear-gradient(135deg, #007bff, #6610f2);
      color: white;
    }
    .hero h1 {
      font-size: 3rem;
      font-weight: bold;
    }
    .hero p {
      font-size: 1.2rem;
    }
    .cta {
      margin-top: 20px;
    }
    footer {
      background-color: #f1f1f1;
      padding: 20px 0;
      text-align: center;
      margin-top: 40px;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">URL Shortener</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
              data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" 
              aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link active" href="/">Home</a></li>
         <% if (!user) { %>
          <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
          <li class="nav-item"><a class="nav-link" href="/signup">Signup</a></li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
        <% } %>
          <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="hero">
    <h1>Welcome to QuickLink</h1>
    <p>Shorten long URLs, manage your links, and share easily. 100% free and fast.</p>
    <div class="cta">
        <% if (!user) { %>
      <a href="/signup" class="btn btn-light btn-lg">Get Started</a>
        <% } else { %>
             <a href="/dashboard" class="btn btn-light btn-lg">Get Started</a>
            <% } %>
    </div>
  </div>

  <!-- Feature Section -->
  <div class="container mt-5 text-center">
    <h2 class="mb-4">Features</h2>
    <div class="row">
      <div class="col-md-4">
        <h4>Shorten URLs</h4>
        <p>Paste your long URL and get a short one instantly.</p>
      </div>
      <div class="col-md-4">
        <h4>Track Links</h4>
        <p>Get analytics on how many people clicked your links (coming soon).</p>
      </div>
      <div class="col-md-4">
        <h4>Secure Access</h4>
        <p>Accounts are protected and URLs are stored securely with MongoDB.</p>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    <p>&copy; 2025 QuickLink URL Shortener. All rights reserved.</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
